name= "this is a virtual  game "


# print(name.find("  "))  # to find the double space in string, if not out will be -1


print(name.replace("  ", " ")) # to replace to double space by single space

print( name ) #strings are immutable which means that you cannot change them (orignal) by running function
